[project]
name = "advanced_file_projects"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
advanced_file_projects = "file_projects.__main__:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
select = ["E", "F", "I", "B"]
